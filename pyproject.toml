[tool.poetry]
name = "beauty-navigator-scraper"
version = "0.1.0"
description = "Парсер косметики для Beauty Navigator (WB, Ozon, ЗЯ)"
authors = []

[tool.poetry.dependencies]
python = "^3.12"
httpx = "^0.27"
selectolax = "^0.3"
pydantic = "^2.0"
tenacity = "^8.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
